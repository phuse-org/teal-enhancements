# Task 1:
# * create a `cdisc_data` object using `ADSL` and `ADAE` from `pharmaverseadam`
# * use `join_keys(created_data)` to ensure that your data has correct `join_keys`

# Task 2:
# * using documentation of `teal.modules.clinical` - https://insightsengineering.github.io/teal.modules.clinical/latest-tag/reference/tm_t_events.html
#   create a `teal` application including the `tm_t_events` module
# * change the default sort criterium to alphanumeric
# * add a title to the module's output using `pre_output`

# Task 3 (additional):
# * using documentation - https://insightsengineering.github.io/teal.modules.clinical/latest-tag/reference/tm_t_events_summary.html
#   create a `teal` application including the `tm_t_events_summary` module.
